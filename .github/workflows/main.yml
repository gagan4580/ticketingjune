name: Deploy to ArgoCD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Update manifest file
        run: |
          sed -i 's/my-docker-registry\/my-react-app:latest/my-docker-registry\/my-react-app:${{ github.sha }}/g' my-react-app/myapp.yaml
          git config --global user.email "gagan4580@yahoo.com"
          git config --global user.name "gagan4580"
          git add .
          git commit -m "Update image tag"
          git push origin main
        env:
          GITHUB_TOKEN: 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 

      - name: Install ArgoCD cli
        run: |
          curl -sSL -o /usr/local/bin/argocd https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
          chmod +x /usr/local/bin/argocd

      - name: Login to ArgoCD
        run: |
          argocd login 127.0.0.1:8081 --username admin --password eH0QS0KqhY52ZF4B --insecure

      - name: Sync application
        run: |
          argocd app sync my-react-app
